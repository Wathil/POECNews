export class JwtResponse {
    accessToken: string; // access token
    categoryId: string;
    id: string;
    loginName: string;
    email: string; // former userName
    accredit: string;
    roles: string[];
}